\documentclass[border=3pt,tikz]{standalone}
% \usepackage[margin=0.25in]{geometry}
\usepackage{pgfplots}
\pgfplotsset{width=10cm,compat=1.9}
\usepackage{amsmath}
\usetikzlibrary{calc}
\usetikzlibrary{arrows.meta} % for arrow size
\pgfplotsset{
    axis line origin/.style args={#1,#2}{
        x filter/.append code={ % Check for empty or filtered out numbers
                \ifx\pgfmathresult\empty\else\pgfmathparse{\pgfmathresult-#1}\fi
            },
        y filter/.append code={
                \ifx\pgfmathresult\empty\else\pgfmathparse{\pgfmathresult-#2}\fi
            },
        xticklabel=\pgfmathparse{\tick+#1}\pgfmathprintnumber{\pgfmathresult},
        yticklabel=\pgfmathparse{\tick+#2}\pgfmathprintnumber{\pgfmathresult},
        every axis/.append style={line width=1.3pt}

    }
}
\begin{document}
\begin{tikzpicture}[scale=1]
    \begin{axis}[
        axis lines=middle,
        xmin = 0, 
        xmax = pi+0.3,
        ymin = -2.5,
        ymax = 2.5,
        domain = 0:pi,
        samples = 100,
        ytick = \empty,
        xtick = \empty,
        label style={font=\Large},
        x label style={at={(current axis.right of origin)},anchor=south},
        y label style={at={(current axis.above origin)},anchor=south},
        ylabel={$V_\text{eff}$ },
        xlabel={$\psi$},
        name = fig1
        ]
    \addplot [blue, line width=1.2]{- cos(x*180/pi)};
    \draw[black] (axis cs: pi, 1.5) -- (axis cs: pi,-0.1) node [below, scale=1.2] {$\pi$};
    \node[scale=1.2] at (axis cs: pi/2, -2) {$\omega= 0$};
    \end{axis}
    
    \begin{axis}[
        axis lines=middle,
        xmin = 0, 
        xmax = pi+0.3,
        ymin = -2.5,
        ymax = 2.5,
        domain = 0:pi,
        samples = 100,
        ytick = \empty,
        xtick = \empty,
        label style={font=\Large},
        x label style={at={(current axis.right of origin)},anchor=south},
        y label style={at={(current axis.above origin)},anchor=south},
        ylabel={$V_\text{eff}$ },
        xlabel={$\psi$},
        name = fig2,
        at=(fig1.right of south east), 
        anchor=left of south west
        ]
    
    \addplot [blue, line width=1.2]{- cos(x*180/pi) - 0.3*(sin(x*180/pi))^2};
    \draw[black] (axis cs: pi, 1.5) -- (axis cs: pi,-0.1) node [below, scale =1.2] {$\pi$};
    \node[scale = 1.2] at (axis cs: pi/2, -2) {$0<\omega^2<\dfrac{g}{a}$};
    \end{axis}
    
\begin{axis}[
        axis lines=middle,
        xmin = 0, 
        xmax = pi+0.3,
        ymin = -2.5,
        ymax = 2.5,
        domain = 0:pi,
        samples = 100,
        ytick = \empty,
        xtick = \empty,
        label style={font=\Large},
        x label style={at={(current axis.right of origin)},anchor=south},
        y label style={at={(current axis.above origin)},anchor=south},
        ylabel={$V_\text{eff}$ },
        xlabel={$\psi$},
        name = fig3,
        at=(fig2.right of south east), 
        anchor=left of south west
        ]
    
    \addplot [blue, line width=1.2]{- cos(x*180/pi) - 1.2*(sin(x*180/pi))^2};
    \draw[black] (axis cs: pi, 1.5) -- (axis cs: pi,-0.1) node [below, scale = 1.2] {$\pi$};
    \node[scale = 1.2] at (axis cs: pi/2, -2) {$\omega^2 > \dfrac{g}{a}$};
    \end{axis}

    \end{tikzpicture}
\end{document}