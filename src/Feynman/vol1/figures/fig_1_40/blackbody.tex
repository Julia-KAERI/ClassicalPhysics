\documentclass[border=10pt]{standalone}

\usepackage{pgfplots,siunitx}
\pgfplotsset{compat=newest}

\def\kB{1.38e-23} % Boltzmann constant
\def\hb{6.62607015e−34} %Planck constant
\def\mu{6.64e-27} % unified atomic mass unit/Dalton (symbols: m_u or Da)

\def\bbs#1{ 1.0e10 * x^3 / exp(\hb * x / (\kB * #1))-1.0}


\def\maxwellboltzmann#1{1000*  4 * pi * (\mu / (2 * pi * \kB * #1))^(3/2) * x^2 * exp(-\mu * x^2 / (2 * \kB * #1))}

\begin{document}

\begin{tikzpicture}

  \begin{axis}[
      domain = 1:8000,
      xmin = 0,
      xmax = 8500,
      % ymin = 0,
      % ymax = 4.3,
      xlabel = {$\omega\,[s^{-1}]$},
      ylabel = $f_\text{MB}(v)$,
      yticklabels=\empty,
      smooth, thick,
      axis lines = left,
      every tick/.style = {thick},
    ]
    
    \addplot[color=cyan, domain=1:800, samples=300]{\bbs{10}};

    %\addplot[color=cyan, domain=1:8000, samples=300]{\maxwellboltzmann{100}};

    % \addplot[color=teal, domain=1:8000, samples=300]{\maxwellboltzmann{300}};

    % \addplot[color=red, domain=1:8000, samples=300]{\maxwellboltzmann{1000}};

    % \legend{\SI{10}{\kelvin}, \SI{100}{\kelvin}, \SI{300}{\kelvin}, \SI{1000}{\kelvin}};

  \end{axis}
\end{tikzpicture}

\end{document}